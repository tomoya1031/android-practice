<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
<!--        <service-->
<!--            android:name=".services.PointService"-->
<!--            android:enabled="true"-->
<!--            android:exported="true"></service>-->
<!--        <service-->
<!--            android:name=".services.PriceService"-->
<!--            android:exported="false" />-->

        <activity
            android:name=".views.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.QrRead"
            android:label="テスト" />
        <activity
            android:name=".views.QrReadView"
            android:label="QRコード読取" />
        <activity android:name=".views.Price" />
        <activity android:name=".views.Point" />
        <activity android:name=".views.Total" />
    </application>

</manifest>